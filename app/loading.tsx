import React from 'react';
import { DotLoader } from 'react-spinners';
type Props = {};

const loading = (props: Props) => {
  return (
    <main className="w-full h-full flex items-center justify-center"></main>
  );
};
export default loading;
